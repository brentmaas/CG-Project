#version 330 core

in vec4 vertexColor;
in float vMass;

out vec4 color;

void main(){
	if(vMass <= 0) discard;
	if (length(gl_PointCoord - 0.5) > 0.5) {
		discard;
	} else
		color = vertexColor;
}